import { globalCss } from "@modulz/design-system";

export const globalStyles = globalCss({
  "body": { margin: 0, fontFamily: "$untitled" },

  "h1, h2, h3, h4, h5": { fontWeight: 500, color: "white" },
});